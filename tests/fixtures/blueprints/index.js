const simple = require('./simple');
const withError = require('./with-error');
const withErrorAndWarning = require('./with-error-and-warnings');
const withMeta = require('./with-meta');
const withDescriptions = require('./with-descriptions');
const withMessages = require('./with-messages');
const withAnchors = require('./with-anchors');
const withNamelessSections = require('./with-nameless-sections');
const withReducedError = require('./with-reduced-error');

module.exports = {
  simple,
  withError,
  withErrorAndWarning,
  withMeta,
  withDescriptions,
  withMessages,
  withAnchors,
  withNamelessSections,
  withReducedError,
};
